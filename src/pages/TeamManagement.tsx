import React from 'react';
import { TeamManagement as TeamManagementComponent } from '@/components/TeamManagement';

const TeamManagement = () => {
  return <TeamManagementComponent />;
};

export default TeamManagement;